'use strict';

angular.module('fullstackApp')
  .controller('ForceMediaGroupCtrl', function ($scope) {
    $scope.items = [{
        'header': 'Overview',
        'body': 'The Force Media Group'
    }];
  });
