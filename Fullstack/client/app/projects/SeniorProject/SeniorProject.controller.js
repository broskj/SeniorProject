'use strict';

angular.module('fullstackApp')
  .controller('SeniorProjectCtrl', function ($scope) {
    $scope.items = [{
        'header': 'Overview',
        'body': 'Senior Project'
    }];
  });
